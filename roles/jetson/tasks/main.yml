---
- name: Configure LXDE enironment
  become: true
  shell: |
    apt install lxde
    dpkg-reconfigure lightdm
    dpkg --configure -a
    apt remove --purge gdm3

# - name: Add keyboard shortcuts for LXDE
#   blockinfile:
#     block: |
#       <!-- Launch LXTerminal with Ctrl+Alt+t-->
#           <keybind key="C-A-t">
#             <action name="Execute">
#               <command>lxterminal</command>
#             </action>
#           </keybind>

- name: Install some apt packages
  become: true
  apt:
    name: "{{ item }}"
    state: present
  loop:
    - nano
    - git
    - exfat-fuse
    - exfat-utils
